import { TableContainer } from '@chakra-ui/react'
import styled from 'styled-components'

const WrapperTableContainer = styled(TableContainer)`
  border: 1px solid lightgray;
  margin-top: 5px;
  border-radius: 5px;
`

export default WrapperTableContainer
